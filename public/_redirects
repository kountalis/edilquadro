# Google Search Console verification file (must be served as-is)
/google2bc1297c3f4bc24f.html /google2bc1297c3f4bc24f.html 200!

# Prerendered static pages exist as files in dist/ (e.g., servizi/index.html)
# Cloudflare Pages automatically serves them - no rewrite rules needed.
# Cloudflare auto-redirects /servizi → /servizi/ (308) when servizi/index.html exists.
# All canonical URLs and sitemap use trailing slash to match this behavior.

# SPA fallback for client-side routes without prerendered files (must be last!)
/* /index.html 200